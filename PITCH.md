# 收藏夹密室 · 一页简介（面向评委）

> **把浏览器里沉睡的收藏夹，变成一间可以亲手逃脱的密室。**
> 上传书签 → 抽取真实网页事实 → LLM 设计 → 机器验证 → 可游玩、可追溯、每次都不一样。

---

## 它解决什么

收藏夹是每个人的"数字遗迹"——上千条随手存下的链接，再也不会被打开。本项目让这些收藏以另一种方式复活：一条 B 站视频成为一盘卡带，一个 wiki 条目成为一台终端，**谜题的答案就藏在这些页面真实的播放量、路径数字和发布日期里**。玩家通关后收到一份"冒险回执"，逐条说明每件物件来自哪条收藏、哪个事实——生成物不是黑盒，而是可追溯的叙事。

## 评委可以重点看的四件事

**1. 生成即验证，不可玩就打回。**
LLM 的设计稿要过两道机器门禁：编译器查结构（多房间全亮、跨房间收束、组合/显形密度、机关参数），**求解器模拟玩家把谜题链走一遍**——卡住即不可玩，卡点直接喂回重设计。不靠"祈祷模型听话"，靠执行验证。

**2. 事实接地是机器可验证的，不是口号。**
设计阶段强制模型为每条素材输出 `sourceFacts`（从网页描述中原样抄录的事实键值），接地检查器逐值 containment 校验——**编造页面事实会被整版打回**。实测谜面引用真实页面数据：B 站视频的弹幕量、硬币数直接进入密码推导。

**3. 多供应商赛马 + 确定性缓存。**
step（router-force advisor）与 GLM 两路并行设计，最先产出可解谜题者胜，败者被信号即时中止止损；时间片内随机抽样 6 条素材保证每次生成不同，缓存键绑定抽样素材——同抽样秒级复用，换抽样必出新关卡。实测生成 70~110 秒（目标 <160s）。

**4. 原作级交互手感。**
对照原版文字密室逃脱逐条移植：物件**原位变身**（不生成新节点）、容器回访显形、房间→容器→道具的空间嵌套、并行房间自由探索；节点出现/变身/位移全动效，机关面板锚定在锁旁边不遮挡战场。

## 现场演示路线（约 3 分钟）

1. `python server/favorites_room_server.py` → 打开 `127.0.0.1:8128`
2. 上传书签导出文件 → 全局清洗（本地规则 + LLM 增量，标记持久化，增量导入秒级就绪）
3. 点击「生成一次未命名冒险」→ 状态栏可见双路赛马与校验反馈流 → 进入密室
4. 点容器找内容物、拖拽组合物件、按谜面推导输密码 → 通关 → **玩家先命名**，再展示候选标题与冒险回执
5. 导出 `.room.json`：scenes/beats/deriveFrom/sourceFacts 全部结构化可读

## 技术架构（一屏）

```
Chrome 书签 → parse → 清洗(本地规则+LLM 增量,标记库持久化)
           → 时间片内随机抽样 6 条 → /fetch-meta 回访真实网页(6 并行,SSRF 防护)
           → designWindow 双路赛马(step+GLM,各 3 轮带修复反馈)
           → compileLevel(scenes 编译) → solveLevel(执行验证) → 挂载游玩
```

- **前端零框架**：四个经典脚本模块各司其职（交互状态机 / 生成管线 / 执行引擎 / 产品壳），IndexedDB 四库本地持久化，书签数据不出本机
- **本地 server**：静态托管 + LLM 代理与密钥保管（前端零密钥）+ `/fetch-meta` 网页回访（SSRF 防护、CORS 锁定、gzip/SPA/多级描述提取）
- **质量基建**：66 条设计原则持续沉淀（`DESIGN-PRINCIPLES.md`），12 个 Playwright 套件覆盖结构/DOM 交互/安全/视觉回归，桩件回归零配额

## 一句话总结

> **内容进入输入 ≠ 内容参与解谜；摘要服务于解谜，证据服务于信任，原网页服务于探索。**
> 这间密室的每一道锁，都能在你的收藏里找到它的出处。

---

仓库与文档：[README](README.md) · [设计原则 P1-P66](DESIGN-PRINCIPLES.md) · [未命名冒险方向](docs/untitled-adventure-direction.md) · 测试：`python ref-game/verify_prison.py`（需先启动本地 server）
