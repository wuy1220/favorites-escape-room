feat: 谜题面板统一详情卡 UI(锚定锁节点旁+📌固定)+全路径展开动画

需求方反馈:
1. 谜题面板位置突兀且不可折叠——与节点详情卡统一:openPuzzlePanel 运行时
   自动升级任意机关面板(keypad 在 index,morse/angle 由 engine 动态创建),
   锚定到被点的锁节点旁(roomUse 记录 __lastUseTarget),四向让位钳制在舞台内;
   📌 固定后不随点击画布空处收起,打开新面板自动收起未固定的旧面板
2. 点击根节点展开看不到过渡动画——justArrived 只在变身路径设置;现已覆盖
   全部显形路径:revealAllRooms(房间/物件/出口)、revealScene、场景点击回访、
   容器开启、环顾四周、roomReveal——凡 hidden→visible 都播 .arrive 出现动画
测试:prison 42/42、bear 30/30、clockwork 39/39、bookmarks 31/31;
  实测:面板锚定转盘锁旁(249,265)、📌 固定后点画布空处面板保持打开
