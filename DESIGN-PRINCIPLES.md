# 密室逃脱关卡设计原则（favorites-escape-room）

> 本文件沉淀本项目从「复刻原作」到「原创关卡」过程中总结出的关卡设计原则。
> 每条原则标注了来源关卡与踩坑反例，供设计与评审时对照检查。
> **维护方式**：每次完成关卡设计/验证后，把新沉淀的原则追加到对应分组，并在文末更新日志；由「定期更新」自动化每周回顾一次。

---

## 一、信息分层（线索设计）

**P1. 环境能观察到的，让环境给；环境看不到的，让道具揭示。**
- 钟面磨损（3 和 6 干净）→ 玩家观察环境自己发现角度目标；展柜锁刻字「答案就在走动的挂钟上」→ 环境引导。
- 反例：说明书直接写「把时针拨到 90°」——玩家看着钟就能推理的常识不需要道具教，显得废话且违和。
- 反例：说明书直接写「密码是 4:35」——最终答案不该由道具直给。

**P2. 道具文本只承载"环境看不出的信息"，一句话能讲清就不写两句。**
- 修钟记录只写「4 月 3 日，钟摆断裂，新配件收在工作台抽屉里」——抽屉里的发条是 hidden，不 reveal 就看不到，这条信息不可替代。
- 红石电路笔记写「红线 300°、黄线 240°、蓝线 180°」——接线角度环境看不到（插孔刻度被油污盖住），必须道具给。
- 反例：说明书复述"后盖能弹开、钟摆断了"——玩家点挂钟自然能看到，全是废话。

**P3. 答案可拆成"两个物件的组合解读"，而不是单一物件直给。**
- 计算器屏幕 4 8 6 + 假名表「从右往左读」→ 密码 684。任何一个物件单独都给不出完整答案。
- 先输 486（直读）会被拒——转换是必要的，玩家体验"组合推理"而非"抄数字"。

**P4. 开场文案 / objective / timeline / hints 不泄露具体答案数字。**
- objective 写「让挂钟走起来，从钟面读出展柜密码」，不写 4:35；timeline 写「看钟面」，不写 435。
- hints 是求助系统，可以从隐到显分级给（先引导观察，再给具体数字），但正文永不直给答案。

**P51. 信息密度铁律：单条文案 ≤2 句，单机关推导 ≤2 线索 1 次换算。**
- 用户实测反馈「展示信息太多、单机关推理过于复杂」（v7.4 废弃医院）——reason 应只讲"这个物件是什么 + 一条可核查线索"，一次换算完成推导；超过就是信息过载。
- 反例：reason 写成资产清单（复述来源/路径/日期）、单机关堆 3+ 条线索多次换算——玩家需要读小作文才能解开一步，新鲜感被耐心耗尽。
- 这是与 P31 正交的约束：P31 管"材料是否真实可核查"，本条管"给多少、多复杂"。

---

## 二、交互与门禁

**P5. 自包含谜题（密码/角度/摩斯）：答案正确 = 通过。前置只影响"玩家怎么知道答案"，不阻塞解锁。**
- 反例：玻璃展柜锁在前置未完成时点击无反应、输对密码也被前置卡住——玩家会觉得被耍。
- 硬门禁只由**素材可得性**承担：combine 没有素材就无物可拖，这是天然硬门禁，不需要额外的 requires 门禁。

**P6. 交互道具随时可用。**
- 密码盘/表盘/摩斯面板：任何时候点开都弹；失败只因为"不知道答案"，不因为"时机不对"。

**P7. UI 不显示答案。**
- 反例：angle 弹窗标签写「目标 300°」——直接剧透。
- 现在：表盘只显示旋钮名（红线/黄线/蓝线）和当前角度，目标由线索推断。

**P8. 真物理感交互优先于同质化点选。**
- 反例：角度用「+ / −」按钮连点。
- 现在：SVG 圆形表盘，拖拽/点按旋转、松手吸附档位（0°=12 点、顺时针，与时钟约定一致）。

**P9. 消耗品用完消失，并有明确日志。**
- 发条接完钟摆、钥匙开门后从画布消失（`consume`），日志「用掉了：生锈的发条。」「用掉了：钥匙。」
- 已消耗物件不列在「已发现」面板，不复活于快照恢复。

**P67. 锁的答案分布公理（2026-08-30 三关重设计）。**
- 锁的答案只允许出现在**别的物件**的详情层里：数据物件给原始读数，规则物件给换算方法，锁自身/premise/objective/hints 一律不含答案。
- 反例（旧稿）：频率面板自己的 reason 写着答案的出处与取法——不读任何物件也能试出锁，密码锁退化为运气面板。
- 多把锁的答案应来自**不同物件的不同字段**（播放量前四位 / 弹幕后三位），玩家必须分别查阅、分别换算。

**P68. 防跳锁闭包公理（2026-08-30 三关重设计）。**
- deliver 的 requires 传递闭包必须覆盖全部非 deliver 步骤——任何一步不在闭包里，玩家就能跳过它直达终点。
- 验证法（verify_demo_v4.py）：①闭包覆盖断言；②逐个把中间步骤 uses 清空，solveLevel 必须改判不可解——"每步都不可跳"要机器证明，不靠自觉。
- 注意：闭包覆盖 ≠ 强制线性。支线并行的关卡同样合法，只要每条支线都汇入 deliver。

**P69. 详情卡避让必须覆盖"后显形"的物件（2026-08-31 资料室关实测）。**
- placePops 四向避让只在**开卡时刻**评估当时的节点集；此后 reveals 飞入的新物件可能恰好落在卡片下方——点击先命中卡片宿主（入口卡例外还会把它吞成环视），物件永远点不到。
- 修复（room02.js anyArrived）：延迟复检（1500ms，飞入动画 0.7s+70ms 交错全部落定后再测量——动画中途 transform 未归零，getBoundingClientRect 不可信）；有覆盖则清该宿主方向记忆重新避让，仍覆盖则收卡兜底。
- 一般教训：**任何"打开时刻"的布局决策，都要对"此后内容集变化"重新生效一次**。

**P70. resultOn 锚定显形——"再点一下变化的节点"（2026-08-31 理清）。**
- beat 带 resultOn 时，reveals 的新物件挂到该节点下（日志:「某某里似乎多了什么，点开看看」）；不带 resultOn 的锁/观察步，新物件挂根上，环顾即可见。
- 手写/生成关卡要有意识地选择：新道具从**产生它的机器里**出来（锚定，更有叙事感），还是直接出现在房间（根显形，更直白）。教程关教的"再点一下变化的节点"就是前者。

---

## 三、结构与流程（原作机制的等价物）

**P10. 节点原位变身，不 spawn 新节点。**
- beat 触发后目标节点**原位更新**（名字/身份/详情），旧状态不共存——原作 `state[] + preClue` 的等价物。
- 硬断言：全程画布 `compiled-result-` 节点数 = 0。

**P11. 回访机制：reveals 不自动弹出。**
- 隐藏物件/容器由 beat `reveals` 标记就绪，但必须「环顾四周」或**点开具体容器**才显形。
- 反例：解锁后一次性把所有东西弹出来（自动弹出）——违背"发现"的乐趣。
- 反例：点关卡入口（根节点）触发全局回访——应只查看关卡说明，回访归回访。

**P12. 非线性：支线无前置、可并行。**
- 书签之屋三支线（看计算器/读红石电路/读假名表）任意顺序；钟表铺角度与记录互不依赖。
- 强制"先读说明书才能操作"是线性，玩家先点了目标机关会迷惑。

**P13. 汇聚式结构：多条独立支线 → 汇聚到单一开锁 → 出口。**
- 借鉴原作第三关「实验室」（配电箱供电 → 终端 → 保险柜 → 隐藏门），比单链条变身更有层次。

**P14. 多身份链：同一节点可连续变身，中间产物身份保留。**
- 挂钟 →「打开后盖的挂钟」→「走动的挂钟」；电报机 →「通电的电报机」→「记下密码的电报机」。
- 中间产物可被后续 beat 引用（`result:<beatId>`），身份用数组保存不覆盖。
- 注意：sequence 按**原始 id** 匹配，`result:` 身份只用于 combine/deliver 引用——fixed-sequence 模板曾用 `result:` 引用顺序锁素材，永远匹配不上导致锁无法完成（e2e_compile 抓出）。

**P15. 消耗品 vs 保留品要分清。**
- 发条/钥匙是一次性工具 → consume 消失；锯子/棍子等复用工具 → 保留。按道具语义决定，不搞一刀切。

---

## 四、叙事与世界

**P16. 道具文本同时承载世界观与线索。**
- 日记/笔记/说明书既讲背景（"4 月 3 日钟摆断裂"）又给信息（"配件在抽屉里"），不是纯摆设。

**P17. 真实素材融入，产生共鸣。**
- 用玩家真实收藏夹（724 条书签）做物件：NandGame、fx-991CNX 计算器、日语假名、红石电路——玩家看到自己的收藏被"活化"，沉浸感远高于虚构物件。

**P18. 主题一致性（适用于明确单一主题的关卡）。**
- 书签之屋：文件夹=容器、书签=物件、出口=地址栏，整个世界观闭环。
- 但这不是所有生成关卡的硬约束。收藏天然跳跃时，强行统一主题会制造虚假关系；此时应遵循 P33-P38，以母题、冒险语法和事后解释建立连贯性。

---

## 五、验证与质量

**P19. 每条 beat 断言"原位变身 + 零新节点"。**
- `verify_*.py` 固定检查：节点名变化正确、`compiled-result-` 节点数 = 0。

**P20. 静态断言防逻辑退化。**
- 断言 reason/objective/timeline 不含具体答案数字（`4:35`、`435`、`90°` 等）；环境线索 reason 必须含观察线索词。

**P21. 断言关键交互行为，不只断言通关。**
- 「前置未完成时密码盘也能弹出（随时可用）」「屏幕直读的 486 被拒」「钥匙用完消失」「回访容器才发现 X」——这些是设计原则的守卫测试。

**P22. 关卡数据结构完整。**
- `validation.valid`、`mechanics` 列表、`timeline` 步骤、`hints` 由隐到显、`targetMinutes` 合理。

**P23. 产物必须有下游（孤儿产物守卫，v6）。**
- combine/sequence 的结果要么被后续 beat 用 `result:` 引用参与下一步，要么变身后的物件本身被继续使用（排水管→棍子→撬锁模式），要么它就是交付物。
- 反例：generated-live 里 `step10` 的序列产物只被自动补的 deliver 使用，`step12` 密码步 even 没挂 expected 被降级——两处都被编译器静默放行，玩家走到一半发现"变身完没下文"。
- 编译器现在对孤儿产物 / 推理锁降级 / 缺 deliver 抛 structural 错误，触发带反馈的整版重写，而不是静默降级成"看似完整的空壳"。

**P24. 编译器不得丢弃谜题语义字段。**
- scenes 分支曾把 LLM 设计里的 `resultOn/product/consume/labels/colors` 全部剥掉，导致所有组合都落在 uses[1]、没有变身名、钥匙不消失——示例关卡的"原位变身感"全部丢失。
- P10/P14/P15 是引擎行为，更是数据语义；任何一条编译路径都必须保留这些字段，否则示例和生成物天然分成两个物种。

**P25. 设计师必须输出谜题链，否则编译器的链优先是空中楼阁（v6.5）。**
- v6 只改了编译器（compileFixedRoom 优先采用 LLM 链），但 UI 生产流程喂给它的是 designWindow(v4)——一个纯文案师，systemPrompt 明令"不要输出 scenes/beats"，schema 里根本没有 beats 字段。结果：LLM 链永远为空，永远静默退回固定模板，v6 的核心价值在生产路径上从未兑现。
- 教训：管线改造要顺着"谁在喂输入"往上查一层；编译器再严格，上游不产链也是白搭。
- 现 designWindow 升级为文案+谜题链双责：sceneLayout 固定三场景物品归位 + v6 骨架硬性要求进 systemPrompt + beats<5 在 designWindow 内部就 retry（不等编译器发现）。

**P26. 静默回退必须暴露给重试回路（__lastChainIssue 全路径写入）。**
- compileFixedRoom 的两条回退路径只有"结构性 throw"写了 `window.__lastChainIssue`；"链不完整(<5 步) return null"那条静默通过，generate() 读到空值误以为成功，三轮自修复回路形同虚设。
- 规则：任何降级/兜底都要留下机器可读的痕迹（调试钩子），且该字段只反映最近一次编译结果，不带上一轮残留。

**P27. 采用 LLM 链时，场景归属必须与链对齐（v6.1 分桶）。**
- 运行时 `sceneCleared` 用 `scenes[si].beatIds` 判断场景完成；v6 初版采用 LLM 链后 beatIds 仍写死模板 id（`fixed-*`），而链 beat id 是 `lsc*-*`——场景 1 永远不成立，关卡卡死第一幕。
- 修法：按 beat 操作素材的固定场景层级（入口 0 / 工作台 1 / 暗格 2）+ `result:` 递归取产出层级 + 链序前缀最大值单调分桶；收尾 beat 强制归暗格（其交付源是 compiledResult 节点，引擎对它豁免跨场景限制）。
- 推广：任何"替换默认结构"的新路径，都要重审结构元数据（id / beatIds / scene 归属）的一致性，不只是 beats 本身。

**P28. 范例模仿胜过规则堆叠（v7 架构翻转的立足点）。**
- 用户用"让 LLM 先复刻原作前两关、再自由创作"做出了满分示例，而生成器堆了 20+ 条规则产出平平。差距的根源不是规则不够，是规则把注意力打碎了：模型在为"逐条合规"优化（凑交叉关键词、选最省事的机关、写时间戳复述），而不是为"做出好谜题"优化。
- v7 做法：把两间已验证关卡（监狱复刻/熊曰情报）的完整数据作为 few-shot 范例喂给设计师，指令压缩到"模仿三样东西：链条接力写法、机关推导方式、reason 交叉笔法"；规则只保留可计算的不变式。设计质量从"立法"回到"示范"。
- 教训：LLM 是模式模仿器，不是规则执行器。想让它产出 X，最可靠的方式是给它看最好的 X，而不是给它描述 X 的一百条戒律。

**P29. 验证靠执行，不靠立法（solveLevel 求解器门禁）。**
- 规则永远预判不了所有失败形态（v6.5 刚修完，v7 第一批就冒出"锁面板拦截观察步"这种没人预想过的问题）。与其继续立法，不如让求解器模拟玩家把 beat 图走一遍：requires 顺序、物件可用性（隐藏/显形/消耗）、场景门禁、deliver 可达性——卡住即不可玩，卡点直接喂回重设计。
- 编译器守卫不废除（孤儿产物/锁参数/空 uses 仍是廉价的第一道网），但权威移到执行验证。修复反馈也从"违反了第 N 条规则"变成"玩家走到第几步会卡住、为什么"——这才是设计师语言。

**P30. 机关的目标物件不得与观察目标重叠（引擎"随时可弹"的必然推论）。**
- 引擎的密码盘/摩斯/角度面板是"随时可弹"的（P21：前置只影响线索可得性，不影响解锁）。推论：若锁装在某个 inspect/revisit 步的目标物件上，该物件的一切点击都会被机关面板拦截，观察步永远无法完成。
- 范例都本能地遵守：prison 的摩斯锁装在变身后的电报机上（result: 产物），密码锁装在没人观察过的大铁箱上；bear-code 的解密终端没有观察步。v7.1 把它做成设计校验 + 求解器 lint（第一批量生成的 r1 正是死于此坑）。

**P31. 推理材料必须来自可核查字段——范例教学的反面副作用（v7.4 事实锚定铁律）。**
- v7 用监狱复刻做 few-shot,而监狱的推理材料(摩斯对照表笔记、生日日记)本来就是虚构道具——模型有样学样,开始虚构"页面事实":海报印着跳跃仰角45°、讲义附录有摩斯对照表、便签边缘有密文。素材的 description 全空时模型手里只有元数据(标题/域名/路径/文件夹/日期),虚构几乎必然发生。
- 用户一眼识破:"用于推理的信息并非来自原网页的事实"。v4 时代"引用必须可核查(逐字出现在 title/urlPath/folder/date)"的锚定规则在 v7 精简时被删,过犹不及。
- 修复:prompt 加事实锚定铁律(推理材料只准来自真实字段;路径数字串如 /opus/351262298288053446 是最佳原料——取位/分段/求和;严禁虚构附录/参数/日期)。教训:换范例时,要审计范例隐含教导的"坏习惯";删规则前先想清楚它拦的是什么。

**P32. 机关参数必须在设计环节验证;降级=高潮消失=整版打回(v7.4)。**
- 角度锁 angles 不是 precision 倍数 → 编译器静默降级成普通 inspect,'调整海报角度'名存实亡,开局高潮凭空消失,玩家找不到机关还以为是自己没看懂。flat 分支此前只记 issue 不打回(scenes 分支早就打回),两条编译路径语义不一致。
- 修复:designWindow 校验机关参数(password expected 3-6 位 / morse code 字符集 / angle angles 均为 precision 倍数),flat 分支降级改 structural。
- 同节点多锁(摩斯启动→密码解锁)是好的叙事结构,但引擎按单一 key 匹配机关,第一把锁变身节点后 key 变 result:*,第二把锁永远打不开——修复为 nodeKeys 全身份匹配(与 inspect 身份修复同类)。

**P33. 主题可以延迟出现，甚至不需要单一主题。**
- 生成前不必让用户选择医院、实验室、图书馆等固定外壳；冒险可以保持未命名，通关后再由玩家命名。
- 无主题不等于无结构：世界可以跳跃，事实边界、交互反馈和通关逻辑必须严格。

**P34. 用母题和冒险语法组织跳跃素材，不强造内容关系。**
- 低关联收藏可以通过重复声音、动作、符号或状态变化产生“押韵”，不必解释成同一题材。
- 可选语法包括变形、朝圣、审判、失忆、官僚迷宫、梦境接力、错误宇宙和无中心选集；语法对玩家隐藏。

**P35. 世界可以做梦，证据不能做梦。**
- 允许把网页化身为物件、天气、人物口吻或空间规则；禁止虚构网页正文、参数和日期。
- 所有关键谜题仍须回指标题、域名、路径、文件夹、收藏时间、描述或重复收藏等可核查事实。

**P36. 惊奇要有预算。**
- 一关只安排 1-2 个改变理解的核心转折，其余交互规则保持稳定。
- 转折应重新解释玩家已经做过的动作，不能靠临时加入新设定制造“意外”。

**P37. 玩家先命名，模型后解释。**
- 通关后先让玩家输入标题，再展示 LLM 的候选标题，避免模型解释成为标准答案。
- 最后提供“冒险回执”，逐项说明真实收藏事实如何转化为物件、事件和母题。

**P38. 意识流质量以“事后成立”衡量。**
- 体验中应不可预先穷尽，体验后应可追溯、可解释。
- “随机、怪异”不是质量；只有当玩家看到回执后能理解创意来源，跳跃才转化为惊喜。

**P39. 多场景不等于多房间。**
- scenes 若只包含标题、物件和 beatIds，它表达的是章节分组，不是空间。
- 空间必须具有包含关系、邻接关系、可达性、开闭状态和回访路径；仅写“资料室/走廊/阁楼”不会自动产生空间感。

**P40. 场景章节与空间图必须分层。**
- scene 负责叙事节奏；space 负责房间、走廊、家具、容器、门和暗格。
- 禁止用 sceneIndex 作为唯一空间门禁，否则任何多房间设计都会退化成线性幕序。
- 反例：compiledHandle 的场景守卫在 parallelRooms 下 sceneIndex 恒 0，把第 2+ 房间的非产物物件全部误拦（「这个物件不在这个场景里」）——并行多房间必须放行场景守卫，全房间自由探索。

**P41. 空间状态必须由谜题效果改变。**
- beat 不应只产出 clue；它还应能 open、unlock、reveal、move 或 transform 空间节点与连接。
- 原作空间感来自“钥匙打开抽屉、抽屉露出暗格、暗门通向旧房间背面”，不是完成一组步骤后切换页面。

**P42. 可回访与递归容器是空间层次的最低门槛。**
- 至少支持房间 → 家具或表面 → 容器 → 道具的递归层级，并保留已经发现的旧空间。
- LLM 生成路径必须真正接入容器执行能力；不能让 containers 只存在于旧平铺分支，而 scenes 路径永远只有“场景 → 物件”一层。

**P43. 内容研究必须先于谜题设计。**
- 当前主链未向设计模型提供网页正文，样本 description 全空；模型只能围绕标题、域名、路径和日期出题。
- 研究阶段应先读取原始收藏页面并形成受限证据包，再让设计阶段选择素材和编写谜题。禁止先编谜题、再搜索材料为既定答案辩护。

**P44. 元数据是备用线索，不是默认谜题原料。**
- 页面正文、标题层级、列表、代码和步骤应优先于域名、路径数字和收藏日期。
- 页面内容不可得时可以使用元数据，但必须标为 metadata-only；不得把 URL 数字伪装成页面页脚、卡背、批注或说明书内容。

**P45. Few-shot 必须区分机制示范与事实示范。**
- 监狱和熊曰范例能示范 combine/result/机关链，却包含人工虚构的日记、摩斯表和密文，不能被模型当作当前网页事实的生成范式。
- 面向内容 grounding 的范例应包含“书签 → 网页证据 → evidenceRefs → 关卡”的完整三元组。

**P46. 事实可追溯必须落到具体证据。**
- deriveFrom 只指向书签 id，不足以证明“页脚写着”“卡背印着”等断言。关键谜面和推理锁应引用具体 excerpt/quote，并通过本地包含检查或更强的证据校验。
- 结构可解和事实可信是两条独立门禁：solver 证明能执行，grounding checker 证明有理由这样解。

**P47. 性能优化优先消除重复工作，不削弱创作和证据链。**
- 主要瓶颈通常是设计模型调用、重复 Prompt、供应商赛马、重试和代理层重复请求；应先压缩上下文、分阶段生成、局部修复、分层缓存和延迟启动备用路线。
- 低推理配置可以用于清洗、证据抽取、JSON 修复、标题和回执；核心创作不应仅为降耗而牺牲空间想象、grounding 或谜题质量。

**P48. 生成性能必须用质量指标一起验收。**
- 至少记录 P50/P95 总耗时、Prompt token、真实上游调用次数、首 token 延迟、重试次数、缓存命中和取消路线。
- 同时记录正文证据引用比例、metadata-only 比例、结构/事实校验通过率和真实 DOM 通关率；单纯更快不代表优化成功。

**P55. 硬规则必须约束玩法语义，不能只约束 action 数量。**
- `combineCount >= 1` 只能制造一个象征性组合，不能保证组合改变物件或空间状态、产生后续用途，或与另一条线索链汇合。
- 生成门禁应检查 action 的效果、下游依赖和玩家可观察变化；否则模型会选择最低成本的合规路径。

**P56. Prompt 的默认范式决定生成物的默认玩法。**
- 如果 prompt 主要强调 password/angle/morse、expected、deriveFrom、resultOn 和 deliver，模型会自然生成“观察 → 开锁 → 交付”。
- 工具作用、容器打开、物件多状态、遮挡移除、空间回访和非锁定操作必须有同等强度的正例和结构要求。

**P57. Few-shot 必须示范目标来源，而不只是目标外观。**
- 人工关卡中的日记、对照表和密文可以示范机关结构，却不能示范网页内容 grounding；否则模型会把元数据物理化为虚构道具线索。
- 面向真实收藏的范例必须包含“网页内容 → 原文证据 → 机制 → evidenceRefs → 回执”的完整链路。

**P58. scene 章节、space 空间和 beat 谜题必须分离。**
- 多个 scenes 或 parallelRooms 只说明有多个章节节点，不代表存在房间、门、走廊、家具、容器和可达性。
- 只有 `parent/children`、`connections`、开闭状态、递归容器和空间效果进入运行时，才算空间层次。

**P59. hidden 是发现状态，不自动等于空间探索。**
- “beat reveals 一个 hidden 素材”只能表达延迟出现，不能表达房间 → 家具 → 抽屉 → 暗格的局部探索。
- 最低空间玩法应包含工具作用于容器或遮挡物、隐藏物被发现、旧空间可回访以及状态变化影响后续操作。

**P60. 质量门禁要分别检查可执行、可追溯和有玩法。**
- solver 检查机械可达，grounding checker 检查证据真实性，玩法检查检查 action 是否造成有意义的状态变化和操作差异；三者不能由同一个结构通过结果替代。
- 仅提高 combine、hidden 或 lock 的数量，不足以证明冒险更丰富。

**P61. 原始网页内容必须先加工，再进入玩家视野。**
- 抓取结果中的统计、账号、导航、URL 和多语言混杂文本不是玩家线索；应先清洗、摘要、翻译并提取少量 `sourceFacts`。
- `reason`、`sourcePreview`、`originalQuote` 和 `evidenceRefs` 各有职责，不能把完整 description 直接拼进详情。

**P62. 内容进入输入不等于内容参与解谜。**
- 每个关键 beat 必须用 `evidenceRefs` 指向具体事实；`deriveFrom` 只表示书签来源，不表示原文证据。
- 需要分别度量内容利用率、证据覆盖率和 metadata-only 比例，不能只看素材是否带有 description。

**P63. UI 详情采用谜面、证据、原网页三层。**
- 当前谜面默认显示且短；来源证据折叠显示；完整 URL、路径和长文本默认隐藏。
- 中文摘要用于理解，原文用于复核；只有实际参与谜题的 URL/路径片段才需要展示。

**P64. 打开原网页必须有具体观察目的。**
- “打开原收藏”应升级为带 `externalTask` 的探索动作，例如确认启动命令、查找分类表或查看制作名单。
- 监听返回游戏只能记录来源已回访，不能证明玩家已阅读页面；核心谜题必须有摘录兜底。

**P65. 页面研究分为核心、增强和奖励三种依赖级别。**
- 核心线索不能依赖在线页面；增强线索打开原网页更容易但有保存摘录；奖励探索可提供额外故事或彩蛋但不阻塞通关。
- 页面失效、变化、登录墙和动态渲染是正常情况，不能让它们破坏主线可玩性。

**P66. 信息密度是功能质量，不只是视觉质量。**
- 详情卡不是原始资料仓库；玩家此刻能做什么应比系统抓到了多少文字更重要。
- 内容加工、来源回执和原网页访问应共同服务“理解、验证、继续行动”三个连续动作。

**P67. 产品重置必须与底层 room reset 分离。**
- `roomReset()` 只应负责基础节点克隆和底层状态初始化；生成关卡按钮必须调用保留当前关卡上下文的 `resetCurrentLevel()`。
- “重置本关”“离开本关”和“删除存档”是三个不同动作，不能通过同一个隐式函数或按钮语义实现。

**P68. 运行态、当前关卡指针和持久化进度必须保持一致。**
- 不能出现画布已回到固定房间、工具栏仍显示生成关卡、`currentLevel` 和 IndexedDB 仍指向旧关卡的半失效状态。
- 所有切换、重置和恢复操作都应明确更新 UI、运行时、localStorage 和 IndexedDB 的责任边界。

**P69. 快照必须恢复行为状态，而不只是完成线索。**
- 除 `started/done/clues` 外，还要恢复机关上下文、空间发现、容器开闭、物件 morph/used/hidden、sequence/inspect 中间态、提示状态、并行房间模式和视图状态。
- 重放必须幂等；任何旧机关回调都不能在重置后作用于新运行态。

**P70. 生命周期任务必须随关卡生命周期结束。**
- 离开或重置关卡时应停止自动保存、命名轮询、未完成生成/抓取请求和相关计时器。
- 发现一个功能定时器由 `start` 创建、但没有由 `showHome`/reset 销毁，应视为生命周期 bug，而不是可忽略的性能问题。

**P71. 展开动画必须建立在稳定的 DOM 生命周期上。**
- 只在 `innerHTML` 中重新创建节点，无法产生旧位置到新位置的 CSS transition；节点必须保留或使用 FLIP 记录前后几何状态。
- 状态变化应批量提交并只渲染一次，避免后续同步渲染在浏览器首次绘制前替换掉带动画类的 DOM。

**P72. 节点出现动画和空间展开动画是两个不同问题。**
- `opacity + scale + translateY` 只是出现动画，不等于子节点从父节点向空间槽位展开。
- 真正展开需要明确起点、终点、空间布局和子节点 stagger；布局坐标不能在首次渲染前直接跳到终点后再期待 transition 生效。

**P73. 动画状态必须通过生命周期事件清理。**
- `justArrived`/`justChanged` 不应在 `roomRender()` 内立即清除；应在对应 DOM 动画完成后清除，或使用一次性渲染 token。
- 同一节点同时发生显形和变身时，必须组合动画或使用不同 CSS 属性，不能让两个 `animation` shorthand 互相覆盖。

**P74. CSS 同名声明和父级 transform 必须纳入动画审查。**
- 同一选择器的后一个 `transition` 会覆盖前一个，新增动画前必须检查完整声明是否保留了位置过渡。
- 画布、节点和详情卡各自使用 transform 时，要区分坐标系，避免动画 transform 与平移/缩放 transform 互相覆盖。

**P75. 动画验收要检查过程，不只检查最终状态。**
- 首次进入、房间/容器/隐藏物显形、物件变身、再次显形和错误操作恢复都要验证动画类、首次绘制时机、位置连续性和遮挡关系。
- `prefers-reduced-motion` 下应保留状态可理解性，但允许动画被压缩；动画关闭不能改变功能语义。

**P76. 生成等待必须展示真实进展，而不是只显示一个 spinner。**
- 生成中的 UI 应显示阶段、已用时间、已完成的素材/研究工作、路线状态和重试原因，让用户知道系统正在做什么。
- 不显示虚假百分比或模型原始思维链；进度只来自真实阶段事件。

**P77. 生成中的中间结果必须可见但不可误用。**
- 可以展示已选素材、来源状态、房间骨架和布置预览，但未经结构、事实和可玩性验证的内容不能伪装成正式关卡。
- 预览与正式运行态必须分离，避免用户在半成品上操作或让半成品进入存档/缓存。

**P78. 生成任务必须可取消且遵守生命周期。**
- 取消、离开和切换文件时应终止在途请求，阻止旧结果写入新关卡或缓存。
- 等待过久可以提供快速版本，但必须明确告知降级范围，不能静默牺牲证据、空间或玩法质量。

**P79. 动态谜题面板必须按真实尺寸进行视口避让。**
- 锚点右侧/左侧的固定估算不能处理角度数量、长标题、颜色标签、摩斯串和移动端可视高度变化。
- 面板显示后应测量真实 rect，在右上/右下/左上/左下候选中选择不溢出视口的位置，并动态设置最大宽高。

**P80. 谜题面板内容必须有横向收缩边界。**
- `.code`、`.kp-slot`、颜色标签、摩斯显示和按钮行都要处理 `min-width:0`、换行、截断和最大长度。
- LLM 动态文本不能直接决定 UI 尺寸；字段应在编译阶段限长，CSS 再提供最后一道边界保护。

**P81. 谜题面板样式应集中管理并覆盖真实断点。**
- 通用 `.modal-card` 与 `.puzzle-panel .modal-card` 的尺寸、padding、max-height 和 overflow 规则必须有明确覆盖关系。
- 桌面、窄屏、画布缩放/平移和虚拟键盘场景都要验证四边可见及主要操作可用。

**P49. 门禁必须覆盖所有进入产物的路径，含缓存回放等旁路（v7.3）。**
- 反例：缓存命中直接编译回放、不过设计校验、不过求解器——门禁只挂在"新鲜生成"路径上，而用户实际拿到关卡还有第二条路（缓存回放），第二条没挂锁等于没锁；被拒的设计稿还被无条件写入 datasets，回放后玩家拿到 4 步无机关的死关卡。
- 规则：缓存命中也过 solveLevel；只有新鲜生成且过求解器的结果才入缓存（失败轮不落档）；任何"门禁/校验"体系都要先审计绕过主路径的旁路——缓存、重放、旧数据迁移。

**P50. 谜面必须有玩法承载（生成器验收的第三条腿）。**
- reason 的每条信息都要有对应玩家动作：写了"交叉引用"就必须有 combine/sequence 让玩家真的把两个物件凑起来，写了"目标角度"就必须有 angle 表盘去拨。文案质量（judge）与可编译可通关（verify_gen）都是必要条件，但文案与玩法脱节就是"生硬"。
- 反例：生成的 reason 大谈交叉引用，beats 却是固定 10 步模板链——"拼成入口线索""两条合成结果"是幽灵产物，玩家做完了也不觉得谜题成立。

---

## 引擎能力速查（编写关卡可用）

| 机制 | 字段 | 说明 |
|---|---|---|
| 组合 | combine | 拖两个素材，目标节点原位变身（resultOn 定目标） |
| 顺序锁 | sequence | 按顺序逐个点击，错序重置 |
| 检查/回访 | inspect/revisit | 点击阅读；reveals 后回访容器显形 |
| 密码盘 | password | expected + colors（颜色标签）；随时可用，正确即通过 |
| 角度表盘 | angle | angles + precision + labels；SVG 表盘拖拽旋转 |
| 摩斯码 | morse | 点/划/分隔输入，正确即通过 |
| 交付 | deliver | 拖最终产物到出口 |
| 容器 | containers | 文件夹/柜子/挂钟等，内部 hidden 物件回访显形 |
| 场景 | scenes | 多幕切换，产物跨场景保留 |
| 隐藏/揭示 | hidden + reveals | beat 完成后标记 revealReady，回访显形 |
| 原位变身 | product + resultOn | 节点名/身份更新，位置不变 |
| 多身份 | result:\<beatId\> | 中间产物可被后续引用，身份数组保留 |
| 消耗 | consume: [ids] | 素材用完消失 |
| 颜色密码 | colors | 密码盘每位上色标签 |
| 真实页面 | url | 物件挂真实 URL，inspect 弹窗显示页面概要 +「打开源收藏」跳转（real-bookmarks/bear-code） |
| 物件角色 | role + roleLabel | clue/tool/lock/reward/transform 语义标记，UI 角色角标与校验使用 |

---

## 六、丰富性手段（原作六关全量复盘）

> 原作共 6 关，机制统计：password×36、use×32、click×26、text×16、angle×7、image×6、timer×3、knock×2、breakable×2、switch/detector/color-sort/morse 各 1。
> 以下是从中提炼的"关卡丰富性"手段，按可移植性排序。

**R1. 叙事驱动线索（最有效的手段）。** 用邮件/短信/资料/日记/备忘录承载剧情 + 线索，玩家"读故事找答案"而非纯解锁。
- 第五关：邮件1（指示炸毁）→ 邮件2（炸弹说明，**解除=反转行动代号**）→ 邮件3（代号已销毁）——三封邮件推进剧情并埋下解法。
- 第六关：短信（交接数据）、个人档案（我是张伟/生日512）、行动指令（组织计划）——整个世界观靠文本立起来。
- 引擎映射：text 类 = inspect beat + reason 长文本。

**R2. 时间压力 + 失败演出。** 炸弹倒计时（5 分钟，超时 cutscene 爆炸失败）制造紧迫感；夜视仪电量 4 分钟耗尽失效。
- 引擎缺口：无 timer/倒计时、无失败过场——未来扩展方向。

**R3. "看不见的东西"需要工具显现。** 铅笔涂便签显影"代号134"、药品粉末显键盘按键 0/2/7、紫外线灯照出日记隐藏页、手机照排水口、金属探测器找隐藏门。
- 共同模式：普通状态下无信息 → 用道具作用后**状态变化暴露信息**。
- 引擎映射：combine + morph 完全可表达（铅笔+便签 → 便签变身"显出痕迹的便签"）。

**R4. 密码的多样性（每种一种思维模型）。** 原作至少 7 种：
| 类型 | 例子 | 思维模型 |
|---|---|---|
| 反转 | 行动代号 134 → 解除输 431 | 顺序反转 |
| 数学约束 | "不被 5 整除的三位偶数" → 702 | 条件筛选 |
| 颜色 | 红2 黄3 蓝9 | 颜色-数字映射 |
| 图形映射 | 书名→形状（神秘百慕大=三角形），A25-B5-C5 → 海报 | 符号解码 |
| 角数映射 | 纸条 35-42-03（角的数量→密码） | 形状计数 |
| 检索编号 | 内部数据检索 138/444/996 | 数据库查询 |
| 身份生日 | 手机锁屏 = 自己生日 512 | 自我认知 |

**R5. 身份谜题（第六关压轴）。** 玩家是"失忆的特工"——手机锁屏密码 = 我的生日，需要先找回"我是谁"（个人档案）才能解锁。自我认知本身就是谜题。

**R6. 检索/数据库谜题。** 输入编号查人物资料（138=执行者、444=高级特工 DD、996=生化研究员），资料串联剧情 → 引出门禁密码/住处地址。
- 引擎映射：password 可模拟（检索框 = 密码盘，expected=编号）。

**R7. NPC 与交易链（第四关）。** 神秘人出现（stealthUntilClue）/消失（removeIfClue）；金币/宝石→神秘人→钳子/牢饭——**物品交换**驱动流程。
- 引擎映射：combine 可近似（金币+神秘人 → morph）。

**R8. 可破坏物（第四关）。** 墙壁裂缝凿 5 次出金币、木床敲 3 次露铁箱——工具 + 次数。
- 引擎缺口：无次数型交互（每次点击都触发，需计数）——可用 sequence 模拟（同一物件点 N 次？不支持），未来扩展。

**R9. 敲击序列（第四/六关）。** 铁窗按提示敲 1-3-2 下；打火机暗格连点 3 次出 U 盘。
- 引擎映射：sequence（不同物件按顺序）近似；同一物件连点计数是缺口。

**R10. 环境推理。** 窗户朝西（看落日）+ 楼层平面图 + 内部文件 → 门禁密码 605。多个环境事实组合推理。
- 引擎映射：多个 inspect 物件 reason 拼线索。

**R11. 多状态物件（第三/六关）。** 实验日记普通 → 紫外线灯照射后显示隐藏页；排水口"看不清"→"照出东西"→"纸团浮起"——一个物件多状态层层解锁。
- 引擎映射：morph 连续变身（已是 P14）。

**R12. 深层嵌套层级。** 物件嵌套 3-4 层（铁箱→铁箱钥匙→老鼠洞→磁铁铁棍），探索有"挖深"感。

**R13. 工具书特性可作谜题。** 书签指向的**真实工具/算法本身**成为谜题机制，不只是当物件摆件——熊曰加密的"整体倒序 + 91 字字典索引"（密文「和既很」→ 倒序「很既和」→ 索引 2-4-6 → 密码 246，直读 264 被拒）、密码的反转、计算器的按键。线索来自真实工具页，而非编造。
- 引擎映射：password 表达算法解码；combine+morph 表达显影（铅笔+便签→显出字迹的情报）；inspect 长 reason 承载工具说明。零引擎改动（bear-code 实战验证）。

### 引擎能力差距对照（未来扩展方向）

| 原作机制 | 引擎现状 | 说明 |
|---|---|---|
| password/angle/morse/combine/sequence/use/click/inspect | ✅ 已支持 | 含 morph/consume/labels/colors 增强 |
| text 叙事 | ✅ inspect reason 长文本 | 已用（钟表铺/书签之屋） |
| image 线索图 | ⚠️ reason 文本替代 | 可考虑未来支持内嵌图片 |
| 显影/揭示工具 | ✅ combine morph 模拟 | 铅笔+便签→显形 |
| 检索编号 | ✅ password 模拟 | 检索框=密码盘 |
| 身份谜题 | ✅ 叙事 + password | 用 reason 立身份 |
| NPC 交易 | ⚠️ combine 近似 | 无独立 NPC 生命周期 |
| 敲击/次数 | ❌ 缺口 | 无重复点击计数 |
| breakable 可破坏 | ❌ 缺口 | 无工具+次数 |
| timer 倒计时 | ❌ 缺口 | 无时间压力 |
| cutscene 过场 | ❌ 缺口 | 无失败/成功演出 |
| detector 探测 | ❌ 缺口 | 无空间探测 |

## 更新日志

- 2026-08-27：初稿。沉淀来源：监狱复刻（P10/P11/P14）、钟表铺三轮迭代（P1-P4、P5-P9、P12）、书签之屋（P13、P17）、angle 表盘重做（P7/P8）。引擎能力表补 consume/labels。
- 2026-08-27：新增「六、丰富性手段（原作六关全量复盘）」。读完原作全部 6 关（config.js 1893 行），提炼 R1-R12：叙事驱动/时间压力/显影工具/密码多样性/身份谜题/检索/交易/可破坏/敲击/环境推理/多状态/深层嵌套；附引擎能力差距对照表（timer/breakable/knock/cutscene/detector 为缺口）。
- 2026-08-27：实战扩充「熊曰情报」关卡，验证 R1+R3+R4 组合可零引擎改动复用。新增原则草案 R24：工具书的特性可作谜题——书签指向的真实工具/算法本身成为谜题机制。
- 2026-08-27（晚）：对照 sample-puzzles 手写示例与网页生成物定位质量差距。新增 P23（孤儿产物守卫）/P24（编译器保留谜题语义字段）：scenes 编译分支此前丢弃 resultOn/product/consume，且静默放行降级锁与断链结构；现改为 structural 错误 + 带反馈整版重写（web 端最多 3 轮，designWindow 接受 repairNote）。designer systemPrompt 增补「v6 谜题骨架硬性要求」。
- 2026-08-27（深夜）：完成 v6 全链路闭环验证（#67 收尾）。修复三处并沉淀 P25/P26/P27：v6.1 场景分桶（LLM 链 beat id 与固定场景 beatIds 对齐，否则关卡卡死第一幕）；`__lastChainIssue` 全路径写入（<5 步静默回退曾令重试回路失效）；v6.5 designWindow 升级为文案+谜题链双责（根因：v4 纯文案师从不出 beats，v6 链优先在生产路径永远落空；另 generate() 成功路径补 break——首轮成功即停，不再白烧 2 次设计调用）。验证：smoke_v6 14/14（broken→模板兜底+issue 有值；good→llm-chain-v6+语义字段保留）；真实 LLM 三轮自修复生效（第 0 轮孤儿产物打回→第 1 轮 llm-chain-v6 采用，8 步链）；生成关卡真实 DOM 通关 13/13（含跨场景产物拖拽、隐藏物件显形回访）；引擎回归 bear 30/30、clockwork 39/39、bookmarks 31/31、prison 42/42 全绿。MODEL_VERSION bump 至 clean-v7-timechain-v2（失效无链旧缓存）。
- 2026-08-27（深夜2）：用户实测生成关卡"体验天差地别"→诊断出四层差距（生成物 s3 空 uses 步卡死第一幕 / 房间皮是写死模板 / 无真机关 / reason 全是时间戳复述）→ 用户点破本质："示例是让 LLM 复刻原作再自由创作的，规则堆叠没切中要害还分散注意力" → **v7 架构翻转**：designWindow 改为范例模仿设计师（few-shot 内嵌 prison+bear-code 两间已验证关卡，指令只剩"模仿三样东西"），generate() 改走 compileLevel 自由创作路径（放弃固定三场景房间皮，premise/objective/hints/化身全部来自设计稿），新增 solveLevel 求解器做执行验证门禁（卡点喂回重设计，3 轮全败退回固定模板保底），compileLevel flat 分支补 sceneName/hidden 保留。首批量发现新失败形态"锁目标与观察目标重叠→机关面板拦截一切点击"（P30）→ v7.1 设计校验+求解器 lint 双重拦截。沉淀 P28（范例模仿胜过规则堆叠）/P29（验证靠执行）/P30。MODEL_VERSION bump 至 freeform-v1。
- 2026-08-28(凌晨):项目被外部会话模块化重构(index.html→js/{pipeline,engine,app,room02,ref-levels}.js + css,git 历史含 baseline);全部 v7 改动保留。用户新反馈:推理材料是编造的(非原网页事实)+信息过载+机关难找。诊断出三层:①范例副作用(v7.4 事实锚定铁律,P31);②flat 分支降级锁不打回 + designWindow 不验证机关参数 → 角度锁静默消失(P32);③同节点双锁第二把永远打不开(引擎单 key 匹配)→ nodeKeys 全身份。reason 元数据复述回潮 → 信息密度铁律(≤2 句,单机关推导 ≤2 线索 1 次换算)。回归 prison 42/42、clockwork 39/39、bear 30/30、solver 6/6。
- 2026-08-29：确立“未命名冒险”方向。固定主题从默认前置条件降为兼容/兜底；新增 P33-P38：延迟命名、母题与冒险语法、意识流的事实边界、惊奇预算、玩家先命名、事后可解释。完整方案见 `docs/untitled-adventure-direction.md`。
- 2026-08-29：定位多房间无空间层次的根因：现有 scenes 是线性章节而非空间图，编译器自动焊接幕序，引擎一次只显示当前场景，容器能力未接入 scenes 生成路径。新增 P39-P42，并在方向文档记录 scene/space 分层与空间图改造标准。
- 2026-08-30：横向复盘三份“未命名冒险”结果，确认硬规则仍被模型以最低成本满足：主要是 inspect + password，combine 缺乏实际用途，scene 仍是章节而非空间，网页正文未进入输入，few-shot 示范偏向人工虚构道具，验证器不检查玩法语义。新增 P55-P60，并记录轻量 harness 与空间图改造边界。
- 2026-08-29（晚）：UI 系统化重设计（深色沉浸密室）。新增 `css/tokens.css` 设计令牌（近黑蓝底色纵深 4 层/琥珀烛光强调/语义与情绪色/衬线标题字体栈/4px 间距标尺/三级阴影/动效时长与缓动/颗粒噪点 data-uri）；`css/styles.css` 全量重写为组件层并吸收全部 JS 内联样式（app.js 主页 style 块、engine.js kp-style/angle-style 三处注入删除，样式单一来源收敛到 css/）。功能 id、状态钩子类（.hidden/.show/.picked/.on/.full/.active/.shake/.arrive/.compiled-*）、节点 data-id 全部保留，ref-game 回归零改动全绿。界面级修复：game-toolbar 与顶栏 HUD 重叠溢出（移至视口底部居中胶囊）、canvas-tools 中文按钮竖排换行、观察目标与 ROOM STATE 挤在舞台右上（目标改左上琥珀引导框）、侧栏浅纸面与暗色舞台割裂（全暗色统一）、原生 file 控件突兀（::file-selector-button 主题化）、disabled 主按钮文字不可读、顶栏 kicker 在产品关卡下残留"Room 02"文案（mountLevel 动态改为"未命名冒险·状态图"，遵守 P37 通关前不显示内部标题）。新增 ref-game/ui_shots.py 全界面截图工具（零配额）作视觉回归基线；12 张界面 judge 视觉验收全过。
- 2026-08-29（深夜）：UI 结构性重设计第二轮（场景空间化）。修复三个结构性 bug：①"子节点不在父节点周围"——编译关卡/导入草案的坐标原是 hydrate 时的索引常量公式（场景物件锚定全局网格、容器物件共用中央槽位、隐藏物件同点堆叠、跨批导入坐标重复），现改为 `roomLayoutBoard()`（js/room02.js）分区摆位：舞台按 zone 子节点数切分区（1/2/3-4/5-6 → 1x1/2x1/2x2/3x2），名牌置分区顶、物件在分区内流式网格，槽位 hydrate 时一次定死（含隐藏物），reveal 只翻显隐永不再挪坐标；新增分区底板层 #zonePlates（极淡虚线框+微光斑）。②"舞台尺寸随侧栏变化"——.layout 单行 stretch 使舞台高度 ≡ max(720px, 侧栏内容)，观察窗/物品栏无限增长把百分比定位的星座整体拉伸；现改为 100vh 应用骨架（shell grid 行 auto+1fr、舞台高度恒定、侧栏内部滚动+内容限高）。③并行房间守卫 bug——compiledHandle 的场景守卫在 parallelRooms 下 sceneIndex 恒 0，把第 2+ 房间的非产物物件全部误拦（"这个物件不在这个场景里"）；现并行模式放行（P40 全房间自由探索）。交互修复：关卡激活后入口牌退场（root/compiled-level 不再占据舞台中央+放射连线消失）；objective 从"现在可以做"任务清单改为"所在位置+单条下一步引导"横幅（pointer-events:none 不拦节点）；进度统一为真实 beat 计数（X / N 步）替代原生 6 状态借用；节点卡片两行化（角色角标+名字，.name 独占文本供回归精确断言）+ role 色条；详情就地化——node-pop 升级为可交互卡（可选中/关闭钮）+ placePops() 四向避让（上/下/左/右挑不遮挡任何节点的方位，就地上卡永不吞邻近节点点击）；拖拽组合实时高亮落点（.drop-ok）；编译关卡提示改读 level.hints（旧逻辑 frontier='imported' 查 ROOM_HINTS 永远返回原生房间无关提示）；「环顾四周」从舞台右下移到顶栏 HUD（#revisitRoom id 不变，回归真实点击依赖）；savePos 只写不读修复（仅原生房间恢复拖拽布局）。主页重排为三步引导（01 选文件→02 偏好→03 时间片，windowPanel id 挂在 step 容器保持 JS 显隐钩子）。测试基建：verify_design_race/verify_verdict_flow 拦截 /fetch-meta（阶段4 desc 富化真实抓外网 4s/条，网络抖动曾把清洗请求推到固定等待窗外、把并发墙钟误判为串行——纯外网依赖 flaky，断言零改动）。全量回归 10 套件全绿；judge 视觉验收 13/13。
- 2026-08-29（深夜2）：judge 三轮视觉验收驱动的两处收尾——①入口节点不能移除只能归位：clockwork 回归用例会二次点击 root（回访机制），`used=true` 退场使其挂；改为挪到舞台左下角（连线收拢、不再占据中央）。②侧栏改"观察窗段内滚 + 提示/物品/日志常驻"布局：长观察文案（prison 身份层数百字）曾把日志段压到一行并切半行（judge 三轮标红），现观察窗在段内滚动、提示盒独立成常驻段（入口永不被长文案埋没）、日志段固定高度完整显示。
- 2026-08-30：UI 第三轮——「暗房间里的纸质档案」(融合两份外部提案:情境抽屉骨架 A + 纸档案皮肤 B)。视觉:深褐木暗室底不变,组件全面纸质化——节点=纸索引卡(暖象牙底/墨字/角色 mono 小字/墨水色左条/图钉/硬偏移阴影)、引导目标=泛黄便签(红左线/微旋转/mono)、弹窗=纸文件(印章 kicker:红底浅字)、主按钮=火漆印、提示盒=便利贴(微倾)、日志/标签=打字机等宽、时间片/情绪色转墨水色调;强调色从琥珀转为火漆红(琥珀降为舞台光晕/进度辅助)。骨架:侧栏四段合并为右缘「档案夹」情境抽屉(案卷/线索/物品/记录四标签)——**推拉式**(打开时舞台缩窄让位,绝不覆盖节点;此前覆盖式曾让抽屉盖住右列节点、拖拽全落在抽屉上,四关挂 3);关卡开始自动展开到线索页;记录/线索标签有未读红点(log()/updateHint() 在抽屉收起时点亮);把手为舞台右缘竖排纸标签。pane 切换用 **opacity 而非 hidden/visibility**——#log/#inventory 的 innerText 被 clockwork/bear 断言依赖,display:none 祖先下 innerText 为空。移动端(≤768px):抽屉改底部上滑整宽面板(z-index 20 盖过节点)、把手变右下胶囊、触控 ≥44px;窄屏(innerWidth<700)分区纵向堆叠+物件单列,超出分区底缘的内容靠画布平移/缩放(levelStart 自动缩至 0.8 倍)。**教训:hydrate 发生在舞台仍 display:none 时,stage.clientWidth 恒 0——视口判定必须用 window.innerWidth**(窄屏误判曾令桌面全盘单列、同槽位叠节点)。顺手修复:模板 div 嵌套错位(product-home 未闭合曾把命名弹窗/工具条解析进主页容器,主页隐藏时连坐消失)+ 主页双栏塌陷 + `[hidden]{display:none!important}`(B1:工具条 hidden 属性曾被 display:flex 覆盖)+ app.js 两处内联 #9aa3ab(B2)+ angleSvg 五处硬编码 SVG 色改 currentColor/var(B3)+ mountLevel 旧 objective 覆写删除(frontier 覆写补触发 compiledObjective,挂载即出关卡口径)。全量回归 11 套件全绿;judge 验收:桌面 13 张 + 移动端 3 张全 pass。
- 2026-08-30（二）：用户否定抽屉形态（"仍是控制面板换皮：大量留白、信息不显著、界面尺寸受限"）→ 第四轮**沉浸式解谜界面**：抽屉整个删除，画布是唯一主角。信息架构按解谜逻辑重排——观察=节点就地详情卡（已有）；提示=画布右上火漆封印圆钮「线索·N」（常驻、显示剩余观察力），点击展开线索便签浮层，浮层内"求一条线索"触发 requestHint；日志=画布左下单行 ticker（log() 同步最新一条+闪现动画），点击展开完整记录浮层；物品清单删除（画布即清单：灰化=已消耗、图钉=可用），#inventory/#helper 移入 .offstage 隐藏容器（**实测 Chromium 对 display:none 元素 innerText 返回 textContent**，clockwork/bear 的日志与物品断言全部安全——此前为保 innerText 而做的 opacity 堆叠+scrollTop 定位方案连同其"文本位置乱动"副作用一并废除）。**关键 bug（用户报告"档案夹关闭后无法打开"同源）：stage 的画布平移 pointerdown 会对一切非节点按下 setPointerCapture，click 被捕获到 stage——挂进 stage 的任何浮层按钮（封印/求线索/ticker）点击全部失效**；修复=排除条件加浮层类。另一间歇 bug：openDb 失败兜底会二次 addUi 造成整套主页重复注入、两层卡片互相拦截点击——addUi 加幂等守卫。全量回归 11 套件全绿；judge 验收 10 张全 pass（信息密度、沉浸感、纸质语言、无破版）。
- 2026-08-30：每周自动化回顾（首轮）。补写三处积压草案与两处反例：**R13**（工具书特性可作谜题，08-27 熊曰草案）、**P49**（门禁必须覆盖缓存回放等旁路，v7.3 教训）、**P50**（谜面必须有玩法承载，verify_gen 教训）、**P51**（信息密度铁律，v7.4 用户"信息过载"反馈）；P14 补 sequence 按原始 id 匹配的 `result:` 身份语义反例、P40 补 parallelRooms 场景守卫误拦反例；引擎速查表补 url / role 两行。复核去重：草案"线索真实存在"已由 P35/P43-P46 覆盖、端到端验收已由 P29 覆盖，不重复写；08-28~08-30 的 v7.4 事实锚定 / 未命名冒险（P33-P38）/ 空间化（P39-P42）/ UI 沉浸化均已入档 P31-P48。
- 2026-08-30（晚）：P61-P66 落地（用户反馈"谜面有网页信息但没参与解谜 + 节点详情是一长串原文"）。**①信息加工**：设计输出新增 `digest`（设计模型写的一句话中文摘要，外文转述）与 `sourceFacts`（≤3 个从 desc/标题/路径**原样抄录**的事实键值）；`identityOf` 不再直出网页原文与完整 URL（裸 URL/多语言串退出详情层）。**②接地检查**（P46/P62 可验证化）：designWindow 校验器逐条验证 sourceFacts 的值必须包含于该素材的 desc/标题/路径文本，改写或编造整版打回；编译落库 `grounding: content|metadata`（P44/P65 依赖级别标记）。**③第三 few-shot 范例**（P45/P57）：REF_C「深夜放映室·透明夏」用真实投稿页数据示范 desc→sourceFacts→谜题链（播放量 1097 末两位 97+硬币 15=9715），test_compile 金标准覆盖（13/13）。**④externalTask** 透传进详情（"回访任务:打开原收藏，…"）。引擎详情三层化：谜面→事实行→摘要→身份行（P63）；UI detailFloat 防长串溢出。goal 新增 digest/接地断言：digest 6/6、未接地 0 处。实测谜面引用真实页面数据（弹幕量/硬币数进密码推导）。**P43 事实更正**：主链自 2026-08-30 起已在生成时回访网页（时间片内随机抽样 6 条→/fetch-meta→desc 进设计输入），"样本 description 全空"不再成立；抓取失败（403/SPA 壳页）仍常态，故 P65 依赖级别与 grounding 标记照旧必要。教训：content 进入输入≠参与解谜——接地必须机器可验证；few-shot 加接地范例比加规则有效（P28 在内容维度的延伸）。
- 2026-08-30（深夜）：用户逐条否定旧三关（频率面板可跳过/面板答案自泄漏/根节点泄漏密码/流程太短玩法单一）→ **推翻重设计 v4**。设计公理三条入档 P67（答案分布：锁自身/premise/objective/hints 零答案，答案只在别的物件的详情层）、P68（防跳锁闭包：deliver requires 传递闭包覆盖全部前序步骤，verify_demo_v4.py 用"逐个清空 uses 必须不可解"机器证明）。三关各主打一种理解动作：A 末班点播台（计量合成：NGU 播放量前四位 1046+弹幕后三位 416，双数字锁+装带组合，档案卡固化数据防实时漂移）、B 知识库建成之夜（语义文字：Obsidian 标语去首词 "your thinking"+hello-algo 标语首词 "动画图解"，双 textMode 文字锁，javascript.js.cn 卡作干扰项）、C 资料室排架日（排序 sequence：javascript.js.cn 课程结构定书序，sequence+盖章组合，hello-algo 作红鲱鱼）。验证三套：泄漏自检（生成器内置）+solveLevel 9/9（可解/闭包覆盖/每步不可跳）+真实 DOM 实玩 31/31（含错误答案被拒、大小写空格归一化通过、错误顺序整组重来）。**过程中修出两个引擎级问题**：P69 详情卡避让不含后显形物件（rc-manual 落在入口卡下点击被吞成环视→anyArrived 延迟 1500ms 复检+重避让+收卡兜底）；P70 resultOn 锚定显形（c5 的借阅章挂在排架台下，必须点排架台而非环顾——不是 bug 是设计，与教程"再点一下变化的节点"闭环）。另发现实玩脚本教训：**飞入动画未落定时拖拽会落空**（bounding_box 含 transform 位移），测试脚本显形后必须等 1.2s。回归：test_solver 6/6、test_compile 14/14、verify_bookmarks 31/31、smoke_room02 无错误。新增 tools/gen_demo_levels_v4.py（生成+泄漏自检一体）、ref-game/verify_demo_v4.py（求解器验证）、ref-game/verify_demo_v4_play.py（真实 DOM 实玩）。
- 2026-08-31：用户截图反馈语义锁 UI 异常 → 两处修复。①文字模式密码盘仍挂数字键盘+位数圆点——圆点泄露答案长度、"清除"只清数字缓冲形同禁用（openKeypad 按 textMode 隐藏 #codeDisplay/#keypad，keyClear 文字模式清空输入框并聚焦，keypadPress 文字模式直接失效）；②容器 id 同时出现在 level.containers 与 level.items 导致同一物件双节点渲染（素材夹出现两个）——约定与教程关对齐：**容器 id 只写 containers，不进 items/controlledIds/selectedItemIds**（gen_demo_levels_v4.py 已改，inspect 容器靠引擎的自检匹配裸 id/全 id，无影响）。回归：solveLevel 9/9、实玩 31/31、容器节点数=1 断言通过。

**P78. 两种显形语法:主动弹出与回访发现(2026-08-31 需求方裁定)。**
- 原作里有些新节点必须**主动回访**父节点才出现(父节点只给闪动反馈);有些则是机关操作的**现实后果**,
  当场自己掉出来——"第二次检索时机器快速运行产生振动,底座的松动底板被震了出来"。
- 引擎实现:物件标 `auto:true` → 其 reveal beat 完成时立即飞入(不进待发现池),
  日志「『松动的底板』从『点播台底座』旁弹了出来」;未标 auto 的维持回访口径。
- 叙事因果链:beat 的 product 变身交代物理原因(检索台→「嗡嗡作响的检索台」)→ auto 显形是其后果。
  一关内两种语法并存:A 关档案/底板=主动弹出,备忘/光碟=回访发现。

---

## 七、精彩感手法(原作复杂关复盘,2026-08-31)

> 背景:用户裁定三关"整体体验仍然很平淡",要求向原作复杂关(m3 实验室/m4 牢房/m5 拆弹/m6 地下室)深挖。
> 逐行重读 config.js 全部六关后的手法盘点——R1-R12 是机制清单,本节是**结构手法**:
> 它们决定玩家"感觉聪明",而不只是"完成了步骤"。

**P71. 显影三兄弟:信息被"藏"进物理状态,用工具把它变出来。**
- 原作:药粉撒键盘→按键痕迹显形(m6);铅笔涂便签→拓出被撕掉的代号(m5);紫外线照日记→隐藏笔迹(m3)。
- 等价实现:combine(工具,目标)→目标原位变身,新 reason 携带显形信息。去污粉擦配电箱=同构。
- 设计要点:被显形的信息必须只此一条路可得(答案被物理遮蔽),且工具本身无害可早得。

**P72. 检索系统:一台终端,多次查询,档案互相引用形成证据链。**
- 原作 m6:检索 138→其档案"直属于编号444"→检索 444→档案给住址门牌号(=门密码);
  一次检索的输出是下一次检索的输入,最终答案埋在两跳之外。
- 等价实现:同一物件挂多把 password 锁(引擎按未完成顺序逐个弹出),先数字后文字;
  前一份档案的 reason 自然提及下一个检索词,玩家自己"想到可以查这个"。
- 这是"信息检索即玩法",比串联锁强:查询词之间是引用关系而非顺序关系。

**P73. 答案的销毁与恢复:直接答案不可读,但痕迹可读。**
- 原作 m5:行动代号邮件"内容已销毁"→铅笔拓便签得「代号134」→按邮件2的规则**反转**输入 431 解除炸弹。
- 双重变换:恢复(拓印)+变换(反转),每步规则都提前交给玩家。禁式:答案直接印在锁旁。

**P74. 敲击与多次交互:计数器机关,探索世界的物理性。**
- 原作:打火机暗格连按 3 次弹 U 盘(m6);铁窗按 1/3/2 节奏敲→神秘人现身(m4);裂缝凿 5 下→金币(m4)。
- 引擎已实现 `action:"knock"`(beat.count 连击,计满变身+reveals)。计数进度不因点别处清零。
- **可供性与知识分离(需求方第二轮裁定)**:把"空响"写在物件上又加门禁,两头不讨好——
  无门禁则探索者提前拿走终局信物直接通关;有门禁则"先前敲不开、做完流程突然能敲开"玩家感到被欺骗。
  原作解法(m6 打火机):知识到来**之前**物件描述完全惰性("一个坏掉的铁制打火机"),早敲无事发生也不撒谎;
  知识到来**之后**,对世界的解读改变,暗格才"出现"。实现:显形一个新节点(松动的底板)而非改旧节点文案。
- 配套:目标物件 reason 要写"敲起来空响"之类的物理暗示,次数线索放在别处(memo/字条)。

**P75. 环境是线索:场景描写的"闲笔"必须参与推理。**
- 原作 m5:窗户 desc"太阳正在缓缓落下"→房间朝西;结合平面图+"6 层南边的休息室,密码为房间号"→推出门禁码 605。
- 三份材料各出一步:环境观察(朝向)+图纸(房间号)+文件(规则)。任何一份单独都不够。
- 反例(我方旧关):场景描写只是氛围,推理解谜全靠卡片对卡片。

**P76. 全局压力与失败态:一个会走完的时钟。**
- 原作 m5:炸弹 5 分钟倒计时贯穿全关,归零播爆炸动画+失败 cutscene;m6 夜视仪电量 4 分钟。
- 效果:把"解谜"变成"抢险",每个动作都有机会成本。引擎缺口(timer/breakable 之外的硬缺口),待实现:
  level.timer 字段→HUD 倒计时→归零置 done+失败态(不销毁进度,允许重来)。

**P77. 证据网与身份叙事:终局要求理解故事,不只是输对密码。**
- 原作 m6:失忆特工在地下室醒来;锁屏密码=自己的生日(自己的档案里);行动指令写给自己(To 编号138);
  全部线索拼出"我是组织编号138,抓的人是对面的 DD,而我失忆前是特工张伟"——门密码=门牌号藏在两跳之外的档案里。
- 手法:文档互相引用(短信署名=档案化名)、身份反转让旧线索重读出新高义、最后一步用叙事信息而非机关信息。
- 我方落地:A 关"压轴"检索词由上一份档案的备注自然引出;更强形态待 LLM 生成路径消化(P72 的证据链结构进 designWindow 范例)。
- 2026-08-31（晚）：用户裁定三关「整体体验仍然很平淡」，要求向原作复杂关深挖 → 逐行重读 config.js 全部六关（此前 R1-R12 只是机制清单），新增「七、精彩感手法」P71-P77：显影三兄弟（药粉显键痕/铅笔拓便签/紫外照日记→combine 变身等价实现）、检索系统（m6 检索138→444 证据链→同一物件多把密码锁+档案备注引出下一检索词）、答案销毁与恢复（m5 代号已销毁→拓印+反转双重变换）、敲击计数机关（m4/m6→引擎新增 action:"knock"，compileRules+点击分支+快照重放三处，计数不因点别处清零）、环境线索（m5 落日→朝西+平面图→605）、全局压力（m5 炸弹 5 分钟/m6 夜视仪电量——引擎硬缺口，列为待实现 timer）、证据网与身份叙事（m6 失忆特工：锁屏密码=自己生日）。**A 关按此重做**（10 beats）：字条定规则→抽屉取出污粉→combine 显影配电箱→合闸通电（inspect 产物变身）→磁带给播放量 104610374→检索台双查询（461=第3-5位 数字锁→档案备注『压轴不在曲库』→文字检索『压轴』→台长备忘）→频率 1046（前四位）→底座暗格 knock×3（P70 锚定：光碟从底座里点出来）→投递。验证：泄漏自检+solveLevel 9/9+实玩 39/39（新增：未通电敲底座不推进、敲两下未开计数生效、第二查询走文字框、错误编号被拒）。回归 solver 6/6、compile 14/14、bookmarks 31/31 全绿。
- 2026-08-31（深夜2）：需求方两轮设计裁定落地。①knock 可供性:P74 补「可供性与知识分离」——底座前期完全惰性(早敲无事也不撒谎),检索『压轴』后以新节点(松动的底板)形式显形,规避「提前拿终局信物」与「门禁欺骗感」两难;②新增 P78 两种显形语法:引擎 triggerReveals 支持 `auto:true` 物件在 beat 完成时主动弹出(检索台→「嗡嗡作响的检索台」变身交代振动因果→「松动的底板」被震得弹出),未标 auto 的维持回访口径——A 关档案/底板走主动弹出,备忘/光碟走回访发现,一关并存两种语法。A 关 10 beats;实玩 42/42、solveLevel 9/9、回归 solver 6/6 compile 14/14 bookmarks 31/31 全绿。

**P79. 机制族排他:一个创意只属于一关(2026-08-31 需求方裁定)。**
- B/C 两关重做时的硬约束:同一创意机制不得重复出现在两关里——重复即"套路感",丰富性来自机制族差异。
- 三关机制族矩阵:A=证据网族(显影 combine/检索多问/暗格 knock/计量数字锁);B=电与光族
  (角度接线台灯+灯光显形+语义文字锁);C=借阅族(容器弹书+顺序扫描链式组合+NPC 现身/对话/交易)。
- 分配前先盘点已用创意清单,再为每关指定一个"主机制族"+1-2 个辅助,族内手法可以复合,族间不交叉。

**P80. 顺序扫描 = 链式组合 enforce 真次序(需求方种子落地)。**
- 原型:借阅系统——书架点击弹出多本书(容器语法),扫描台要求按索书号升序逐本扫描。
- 实现:每个扫描步是一个 combine(书,扫描台),requires 链接到上一步;乱序拖上去无任何反应
  (beatReady 不过,规则不匹配)——次序是**物理 enforce** 而非文案提示。
- 扫描台连续变身受理进度(「扫描台 · 已受理 1/3→2/3→3/3」)= 过程反馈;最后一步的 reveals
  挂到远处节点(auto)= 节点树另一端的巧合事件(广播响,值班员现身)。

**P81. NPC 三件套:现身/对话/交易(原作 m4 神秘人+m3 丧尸语法)。**
- 现身:机关完成后的 auto 显形(巧合事件,叙事交代他从哪来——『端着茶杯从借阅台那头踱了过来』);
- 对话:点击即弹详情卡,reason 直接写成台词(含下一步的钩子——『章不外借,除非……』);
- 交易:combine(信物,NPC)→NPC 原位变身(「捧着热茶的值班员」)+auto 递出奖励。
- 交易信物必须早在场内且当时无用途(保温壶),玩家读到对话才回溯它的意义——知识重读世界。
- 2026-08-31(深夜3):需求方裁定『一个创意不能重复用在 B/C 两关』+借阅系统种子(书架弹书/扫描顺序/NPC 对话)→B/C 推翻重做 v3。B=电与光族:守库人故意弄坏台灯,皱便签给规则(接回钟面上的家)、端子盘给数据(红3蓝7黄11),angle 三表盘接线(90/210/330),灯亮→照亮墙上便签(auto 灯光显形)→语义锁开门,藏书票上桌交班。C=借阅族:双书架容器弹出 4 本(绿标 3+红标干扰),守馆手册定流程(索书号升序扫描),链式组合 enforce 真次序(乱序拖上去无反应),扫描台连续变身受理进度,3/3 后广播响→值班员 auto 现身(节点树另一端巧合事件),对话→保温壶交易→借阅章→盖章交单。**引擎修复**:flat 分支 level.items.slice(0,12) 容量上限把第 13+ 物件整个裁掉(C 关 14 物件,章/提货单无声消失)→放宽 18(摆板后位置由 roomLayoutBoard 管理,旧 3×4 网格坐标公式只是初值)。泄漏自检扩展(B 角度答案/C 索书号只许出现在书脊数据卡)。验证:solveLevel 9/9+实玩 56/56(含全拨错灯不亮/红标书无受理/乱序被拒/NPC 未现身断言)+回归 solver 6/6 compile 14/14 bookmarks 31/31。

**P82. 用页面「讲的内容」出谜面,而不是页面的元数据(2026-08-31 需求方裁定)。**
- 反例(我方 C 关 v3):索书号 Z-02/Z-05/Z-09 是编造的,书脊引文只是装饰——"抓到数字就乱编"换了个马甲。
- 修正方向:扫描机收书后出**校验题**,题目考的是页面讲的内容——
  ①答案在卡片原话里(MDN 指南说『想深入了解,请参阅 JavaScript 参考』→校验题问参阅什么);
  ②答案在简介里(『主课程包含 2 部分』→考数字);
  ③答案在**原页面**里(hello-algo 3.1 节把数组链表栈队列归为『线性』结构——卡片写回访任务
  『打开原收藏翻到 3.1 节』,玩家真的打开网页找答案)——这是产品核心循环进谜题。
- 元数据(desc/标题/路径)负责**接地与引路**,页面讲授的内容负责**谜面本身**;两者缺一就是
  要么编造、要么复读。

**P83. 容器与手册的门禁边界(P74 在容器上的应用)。**
- 容器节点 id 只写 containers(教程关约定)——写进 items 会双节点渲染(C 关书架重复,实测);
- 容器「打开」是**视觉可得**交互,不该被流程类 beat 门禁(先读手册才能点书架=玩家会困惑
  明明可见却点不动);手册给的是**规则知识**(扫描次序/校验规则),不是开架许可。
- 2026-08-31(深夜4):需求方两点反馈落地。①资料室双书架节点=容器 id 又写进了 items(P83 第一条),且翻书架被『先读手册』门禁——去掉,容器打开是视觉可得交互,手册只供规则;②『又回到不用网页内容做谜面』——编造的索书号推倒,改为**扫描校验题**:收书后机器出题,答案分别来自 MDN 卡片原话(『参阅 JavaScript 参考』)、教程简介(『2 部分』)、hello-algo 原页面 3.1 节(线性/非线性,经回访任务引导打开原收藏——核心循环进谜题,P82)。C 关 17 beats;solveLevel 9/9、实玩 68/68(新增:书架无重复/无手册可翻/三道题错误答案全被拒)、回归 solver 6/6 compile 14/14 bookmarks 31/31。
- 2026-08-31(深夜5):三点收尾。①示例收藏夹导出:tools/export_demo_html.py → fixtures/demo-collection.html(Netscape 格式,书签栏+5 个时段文件夹,可直接导入浏览器/产品导入流程);②B 关三审(用户:交互平淡/没后文/裸机制锁)→ v3 重做:撕信拼合(combine, m5 碎纸语法)→拼好的信 auto 展开(规则载体,绕开 morph 不换 reason 的限制)→angle 接线→**灯光后文连锁**(墙显形+标签屏通电)→双语义锁→**台历翻页→接任书**(叙事 payoff,deliver=接下委托,不是插票);③B/C 房间化:容器即房间,根层只露 3-4 个容器(素材夹/书桌/正门 + 书架×2/借阅台),单次子节点 ≤4。**引擎侧:容器节点补 compiledAuto(此前 auto 只对物件生效,隐藏容器无法主动显形——灯光后文依赖此);flat 编译上限 24。P83 三次违例(素材夹/怀旧柜/借阅台/书架×2/守库人的墙)后固化为生成器机器检查(容器 id 出现在 items 即 FAIL)。B 关修复中证实:飞入动画未落定时的点击会丢(NPC c14),实玩断言前须等动画。验证:solveLevel 9/9 + 实玩 77/77 + 回归 solver 6/6 compile 14/14 bookmarks 31/31。
- 2026-08-31(深夜6):**示例关经验固化进 LLM 生成管线**。①designWindow:action 枚举加 knock(含 count 2-5),物件透传 auto/arrive_text;提示词新增【手法菜单】(显影/检索多问/敲击/主动显形/顺序扫描/NPC/环境线索/校验题八族,beat 级写法+范例,机制族排他)与【答案与文案铁律】(P67/P74/P82/叙事收束);②compileLevel 校验器:P67 答案泄漏机器检查(password expected 不得出现在锁自身 reason 与 premise/objective/hints)+knock 配额(≤1)与 P74 文案禁令(连敲/三下/连按/多敲出现即降级);③适配并行会话的导入净化器重构:IMPORT_ACTIONS 补 knock,expected 双轨(纯数字剥 6 位/语义锁文字保留 40 字)。排障记录:提示词以单引号字符串拼接,插入文本含 ASCII 引号与真实换行都会打碎语法——用 os.replace 原子写+浏览器 new Function 编译验证;校验器首版误插到 sceneBeats 不在的作用域(ReferenceError),改为编译后变量自洽。验证:test_compile 14/14、solver 6/6、实玩 77/77、bookmarks 31/31;P67 烟雾(把密码写进锁自己的 reason)确认被打回。

**P84. 选择题交互:备选节点拖到题干节点(2026-08-31 需求方裁定)。**
- 文字输入答题体验差,且扫描消耗书后玩家失去参照物——双重问题。
- 改法:①扫描**不消耗**书,书留作参照物(答案的出处一直在场);②扫描台原位变身显示题干
  (「校验中:该参阅什么?」);③auto 弹出 3 张备选标签节点;④拖**正确的**备选到题干=combine 受理,
  拖错的物理无反应(beatReady/配对不匹配);⑤作答后备选与书一起 consume(归架)。
- 引擎零改动:备选=combine 配对(consume 全组),题干=机器连续变身,备选=auto 显形。
- 白名单注记:选择题的正确答案必然出现在备选节点文案里,泄漏自检需为备选 id 开白名单
  (干扰项『非线性』含子串『线性』同理)。
- 2026-08-31(深夜7):需求方两点反馈→C 关 v5。①书架被门禁挡住无法直点——推门即视线进入,c1 的 reveals 对三房间容器标 auto,推门后直接亮出(P78 主动语法,不再等环顾);②扫描消耗书失去参照+打字答题体验差——改**选择题**:扫描不消耗书(留作参照),扫描台变身显示题干,auto 弹 3 张备选标签,拖正确备选到扫描台=受理(consume 全组+书一起归架),拖错物理无反应。引擎零改动(备选=combine 配对)。泄漏自检为备选节点开白名单(『非线性』含『线性』子串)。验证:solveLevel 9/9+实玩 74/74(推门 auto 亮房间/书留参照/错误备选无反应×3/归架消失)+回归 solver 6/6 compile 14/14 bookmarks 31/31。

